# Casting

```cs
// Casting : If no magnitude is lost casting happens automatically, but otherwise it must be done like this

double pi = 3.14;
int intPi = (int)pi; // put the data type to convert to between braces
```
