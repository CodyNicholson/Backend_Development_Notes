# Ternary Operator

```cs
// Ternary Operator

// This is a short-hand if else statements
// if the conditional is true, canDrive becomes true
// if the conditional is flase, canDrive becomes false
bool canDrive = age >= 16 ? true : false;
```
